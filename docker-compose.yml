name: docker-compose

services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    image: django-image
    container_name: django-container
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      # - db-data:/app/db

